= render :partial => '/admin/menu' 
%h2 New users
- form_for([:admin,@user]) do |form|
  = render :partial => 'form', :locals => {:form => form}
  %p= form.submit "Create", :disable_with => 'Please wait...' 

%p= link_to 'Back', admin_users_path
